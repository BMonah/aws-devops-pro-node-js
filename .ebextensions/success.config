container_commands:
  signal_success:
    command: >
      /opt/elasticbeanstalk/bin/cfn-signal -e 0
      --stack ${AWS::StackName}
      --resource AWSEBAutoScalingGroup
      --region ${AWS::Region}

